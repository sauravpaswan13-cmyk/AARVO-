import Fastify from 'fastify';
// NOTE: payment finalization is intentionally kept in the existing server implementation.
// This placeholder update is unsafe because it would replace the file, so no deployment is made.