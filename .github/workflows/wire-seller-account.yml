name: AARVO Seller UI

on:
  workflow_dispatch:

jobs:
  seller-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Seller UI automation disabled
        run: echo "Seller UI is maintained in source; no automatic overwrite is performed."
