name: AARVO Splash UI

on:
  workflow_dispatch:

jobs:
  splash-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Splash automation disabled
        run: echo "Splash UI is maintained in source; no automatic overwrite is performed."
