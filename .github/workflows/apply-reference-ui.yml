name: AARVO Reference UI

on:
  workflow_dispatch:

jobs:
  reference-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Reference UI automation disabled
        run: echo "Reference UI is maintained in source; no automatic overwrite is performed."
